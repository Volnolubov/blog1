<%= render 'shared/breadcrumb', category: @category %>
<h1 class="h1_title"><%= @category.name %></h1>

<section id="posts">
  <%= render @posts %>
</section>

<div class="digg_pagination">
    <div class="page_info">
     
    </div>
    <%= will_paginate @posts, :container => false %> 
</div>